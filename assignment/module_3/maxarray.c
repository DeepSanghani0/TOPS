#include<stdio.h>
int main(){
    int i;
    int array[10];
    for (int i = 0; i < 11; i++)
    {
        printf("enter array value array[%d]",array[i]);
        scanf("%d",&array[i]);
    }
    for (int j = 0; j < 11; j++)
    {
        printf("%d",array[i]);
    }
    
    
    return 0;
}